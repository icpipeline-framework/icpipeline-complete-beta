This is a sanity check to confirm that this *cloudconf* directory isn't doing very much at this early stage.

At the moment it holds just an installer-generated json input file for generating a task definition in AWS.

Going forward, we intend to build out modular support across multiple cloud providers, etc.  In that context, we expect that *cloudconf* will play a larger role in the framework.